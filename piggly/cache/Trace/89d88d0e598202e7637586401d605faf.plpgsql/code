BEGIN
    perform public.piggly_branch($PIGGLY$c15fd3ad794db77c$PIGGLY$);
    CREATE TEMP TABLE _____coltmp___ AS
    SELECT $1[i]
    FROM generate_series(array_lower($1, 1), array_upper($1, 1)) s(i);
    EXECUTE 'ALTER TABLE _____coltmp___ RENAME TO ' || $2;
    perform public.piggly_branch($PIGGLY$af1f5441fd0ab4a6$PIGGLY$);return $2;
END;