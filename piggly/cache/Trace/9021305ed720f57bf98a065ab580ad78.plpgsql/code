DECLARE
    res boolean;
BEGIN
    perform public.piggly_branch($PIGGLY$95f12b42fbaad241$PIGGLY$);
    SELECT x.indisunique
      FROM pg_catalog.pg_index x
      JOIN pg_catalog.pg_class ct ON ct.oid = x.indrelid
      JOIN pg_catalog.pg_class ci ON ci.oid = x.indexrelid
     WHERE ct.relname = $1
       AND ci.relname = $2
       AND pg_catalog.pg_table_is_visible(ct.oid)
      INTO res;

      perform public.piggly_branch($PIGGLY$a17dc56d4e85c999$PIGGLY$);RETURN ok(
          COALESCE(res, false),
          'Index ' || quote_ident($2) || ' should be unique'
      );
END;